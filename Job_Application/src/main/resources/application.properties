# Application name
spring.application.name=Job_Application

# Server port
server.port=8081

# MySQL database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/job_table?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=PSingam@007
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate and JPA configuration
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Disable H2 console (not needed for MySQL)
spring.h2.console.enabled=false